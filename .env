# ─────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT environment (do NOT commit real secrets)
# Loaded automatically by settings.py via python-dotenv.
# ─────────────────────────────────────────────────────────────

# Django
SECRET_KEY=django-insecure-2#3-g0l-cup%#7zweh=7#77fp=(%j%37=0g8(xvn&f6dag*n)b
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database — local PostgreSQL running in Docker (container: lost_found_pg)
POSTGRES_DB=lost_found_ai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# POSTGRES_SSLMODE=            (leave empty locally; set to "require" for Supabase)

# CORS — the local Vite dev server origin
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=

# Razorpay — leave blank to run the reward flow in demo mode
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
