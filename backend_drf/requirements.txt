# ── Web / API ───────────────────────────────────────────────
Django>=5.0
djangorestframework
djangorestframework-simplejwt
django-cors-headers
python-dotenv
psycopg[binary]
razorpay
drf-spectacular   # OpenAPI 3 schema + Swagger UI / ReDoc
dnspython         # MX lookup to reject non-existent email domains
requests          # Brevo HTTPS email API (SMTP is blocked on HF Spaces)

# ── Production serving ───────────────────────────────────────
gunicorn          # WSGI server for production (Hugging Face Spaces)
whitenoise        # serves Django admin / DRF static files without nginx

# ── Media storage (Supabase Storage, S3-compatible) ──────────
django-storages   # pluggable file storage backends
boto3             # S3 client used by django-storages for Supabase Storage

# ── AI matching engine ──────────────────────────────────────
# Models download automatically from the Hugging Face hub on first run and
# cache locally. torch auto-detects CUDA (local GPU) and falls back to CPU
# (e.g. Hugging Face Spaces free tier).
sentence-transformers
easyocr
torch
torchvision
transformers
Pillow
